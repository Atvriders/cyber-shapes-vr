/**
 * Thin re-export of shared contracts.
 * Client-only render handles (Three.js mesh/group/light) will be added in A5.
 */
export type { ShapeType, RenderMode, Shape, World } from '@cyber-shapes/shared';
