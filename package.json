{
  "name": "cyber-shapes-vr",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:soak": "SOAK=1 vitest run soak.test.ts",
    "lint": "eslint .",
    "format": "prettier -w .",
    "typecheck": "tsc -b",
    "build": "npm run build --workspaces --if-present",
    "size": "node tools/check-bundle-size.mjs",
    "preflight": "node tools/preflight/cli.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "jsdom": "^25.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
